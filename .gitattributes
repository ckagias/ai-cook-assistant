# Shell scripts must keep LF even on a Windows checkout (core.autocrlf=true): with CRLF, bash
# - in WSL especially - fails with "$'\r': command not found".
*.sh   text eol=lf
# Windows-only launchers are happiest with CRLF.
*.cmd  text eol=crlf
*.ps1  text eol=crlf
