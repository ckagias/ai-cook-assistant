fastapi>=0.115
uvicorn[standard]>=0.30
anthropic>=0.40
openai>=1.60
google-genai>=1.0
pydantic>=2.7
python-dotenv>=1.0
httpx>=0.27
Pillow>=10.0
pytest>=8.0
# Any-URL recipe import (schema.org / JSON-LD parsing), MIT. Fetching stays in app/importers/http.py.
recipe-scrapers==15.12.0
# LAN HTTPS certificate for setup-window.sh/.ps1 (scripts/lan.py) - no openssl needed.
cryptography>=42
# ASCII + PNG QR for the phone pairing URL (scripts/pairing_qr.py). Pillow already present.
qrcode>=8.0
